package com.sebcode.msproducts.security.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS is handled centrally in SecurityConfig via CorsConfigurationSource
}
